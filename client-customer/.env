# Bỏ kiểm tra hostname (chỉ nên dùng trong dev/test)
DANGEROUSLY_DISABLE_HOST_CHECK=true

# ✅ Dùng khi chạy trong Docker (mặc định khi production hoặc test bằng Docker)
REACT_APP_API_BASE_URL=http://backend:3000

# ❌ Không dùng khi chạy trong Docker
# REACT_APP_API_BASE_URL=http://localhost:3000

# ✅ Dùng khi deploy production thực sự (dùng domain thật)
# REACT_APP_API_BASE_URL=https://api.yourdomain.com
